# 智汇于庄 · ai-service (FastAPI 大模型 / RAG 服务) 依赖清单
# Python 3.11+（本地验证环境 3.13.3）

# --- Web 框架 ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
# multipart 表单解析：app/api/knowledge.py 的 /knowledge/docs/upload 使用 File/Form，
# 该依赖由 FastAPI 内部按需加载（项目代码不直接 import，故易漏）。
# FastAPI>=0.115 在【注册含 Form/File 的路由】时即强制校验，缺失会导致应用启动即失败：
#   RuntimeError: Form data requires "python-multipart" to be installed.
python-multipart>=0.0.9,<1.0.0

# --- 数据校验 / 配置 ---
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0

# --- PostgreSQL + pgvector（业务库 / 向量库共用）---
# psycopg[binary,pool]：psycopg3 二进制驱动 + psycopg_pool 连接池
psycopg[binary,pool]>=3.2.3,<4.0.0
pgvector>=0.3.6,<1.0.0

# --- DeepSeek / OpenAI 兼容 SDK（LLM + Embedding）---
openai>=1.55.0,<2.0.0

# --- 可选：PDF 解析（未安装时切片器会给出安装提示，不影响 txt/md 入库）---
# pypdf>=4.0.0,<6.0.0
